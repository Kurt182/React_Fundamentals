// Props
// Short for properties
// Parameter od a component function that get passed into the component to aid in its reueability.
// Dynamic data can be assigned to child components.

// import App from react

// im;port React from 'react';

const NameProp = (props) => {  // property returning React element
    return <h1>Hello,</h1>
}

export default NameProp;